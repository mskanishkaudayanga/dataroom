Cypress.LocalStorage.clear = function (keys, ls, rs) {
    // keys to be preserved
    // const keysToKeep = ['userDataStore', 'agentsStore', 'filterStore', 'leadsFilterStore', 'leadsQualifiedStore', 'leadsStore', 'userRolesStore'];
    // if (keysToKeep.some(key => keys.includes(key))) {
    //     return;
    // }
}
